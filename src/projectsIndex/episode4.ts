import {makeProject} from '@motion-canvas/core';

import opening from '../scenes/episode4/opening?scene';
import act1 from '../scenes/episode4/act1?scene';

export default makeProject({
  scenes: [opening, act1]
});
